# Dependencies
node_modules/
package-lock.json
yarn.lock

# Environment variables (secrets — never commit; .env.example is tracked)
.env
.env.local
.env.*.local

# Web Push VAPID keys (contains private key — must never be committed)
vapid.json

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs
dist/
build/

# Temporary files
.tmp/
temp/
